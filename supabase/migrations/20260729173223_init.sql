CREATE TABLE public.accounts (id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL, created_at timestamp with time zone DEFAULT now() NOT NULL, name text NOT NULL, description text);
ALTER TABLE public.accounts ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.accounts ADD CONSTRAINT accounts_name_key UNIQUE (name);
ALTER TABLE public.accounts ADD CONSTRAINT accounts_pkey PRIMARY KEY (id);
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.accounts TO anon;
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.accounts TO authenticated;
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.accounts TO service_role;
CREATE TABLE public.members (id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL, created_at timestamp with time zone DEFAULT now() NOT NULL, account_id bigint, user_id uuid);
ALTER TABLE public.members ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.members ADD CONSTRAINT members_account_id_fkey FOREIGN KEY (account_id) REFERENCES public.accounts(id);
ALTER TABLE public.members ADD CONSTRAINT members_pkey PRIMARY KEY (id);
ALTER TABLE public.members ADD CONSTRAINT members_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id);
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.members TO anon;
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.members TO authenticated;
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.members TO service_role;
CREATE TABLE public.transactions (id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL, created_at timestamp with time zone DEFAULT now() NOT NULL, from_account_id bigint NOT NULL, to_account_id bigint NOT NULL, amount numeric NOT NULL, description text);
ALTER TABLE public.transactions ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.transactions ADD CONSTRAINT transactions_from_account_id_fkey FOREIGN KEY (from_account_id) REFERENCES public.accounts(id);
ALTER TABLE public.transactions ADD CONSTRAINT transactions_pkey PRIMARY KEY (id);
ALTER TABLE public.transactions ADD CONSTRAINT transactions_to_account_id_fkey FOREIGN KEY (to_account_id) REFERENCES public.accounts(id);
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.transactions TO anon;
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.transactions TO authenticated;
GRANT MAINTAIN, REFERENCES, TRIGGER, TRUNCATE ON public.transactions TO service_role;
